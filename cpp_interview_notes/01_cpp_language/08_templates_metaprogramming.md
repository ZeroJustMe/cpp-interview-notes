# 08. 模板进阶、SFINAE、类型萃取、元编程基础

## 1. 什么是 SFINAE？

### 标准回答
SFINAE（Substitution Failure Is Not An Error）指模板参数替换失败时，不直接报错，而是让该模板候选失效，编译器继续选择其它匹配项。

## 2. enable_if 的作用是什么？

### 标准回答
`std::enable_if` 常用于基于类型条件启用/禁用某些模板重载。

## 3. 类型萃取（type traits）有什么用？

### 标准回答
类型萃取用于在编译期获取类型性质，如：
- 是否是整数类型
- 是否可拷贝构造
- 是否有平凡析构

### 常见工具
- `std::is_integral`
- `std::is_same`
- `std::is_trivially_copyable`
- `std::remove_reference`

## 4. 什么是模板元编程？

### 标准回答
利用模板实例化和编译期常量机制，在编译期完成计算、分派和约束的一类编程方式。

## 5. 模板为什么编译慢、报错难看？

- 实例化链条长
- 重载决议复杂
- 错误经常发生在替换后的深层上下文

## 6. concepts 相比 SFINAE 有什么好处？

- 语义更清晰
- 错误信息更友好
- 接口约束更直接

## 7. 面试提示

如果岗位偏现代 C++、基础库或框架开发，这部分很容易追问；如果没项目经验，至少要会讲：
- SFINAE 是什么
- enable_if 怎么做约束
- concepts 为什么更好
